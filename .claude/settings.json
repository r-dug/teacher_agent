{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "path=$(cat | jq -r '.tool_input.file_path // empty'); if echo \"$path\" | grep -qE '(\\.env$|\\.env\\.|uv\\.lock$|package-lock\\.json$|migrate_v[0-9]+\\.py$)'; then echo \"Protected file: $path\" >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
